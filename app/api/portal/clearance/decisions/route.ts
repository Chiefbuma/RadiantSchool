import{NextResponse}from"next/server";import{decideClearance}from"@/lib/portal-completion";import{getCurrentUser}from"@/lib/auth";import{PortalError,portalErrorResponse,requestId}from"@/lib/portal-security";export async function POST(r:Request){const c=requestId(r);try{const u=await getCurrentUser();if(!u)throw new PortalError(401,"Authentication required");return NextResponse.json(await decideClearance(await r.json(),u,c));}catch(e){return portalErrorResponse(e,c);}}
